import React, { useState, useEffect } from 'react';
import axios from 'axios';
import DualPaneModal from './DualPaneModal';
import PDFPreviewPane from './PDFPreviewPane';
import { Plus, Trash2 } from 'lucide-react';

const BACKEND_URL = process.env.REACT_APP_BACKEND_URL;

/**
 * OnboardingWorkflow - Generate and send appointment letter
 */
const OnboardingWorkflow = ({ isOpen, onClose, candidate, job, onSuccess }) => {
  const [formData, setFormData] = useState({
    designation: job?.title || '',
    department: job?.department || '',
    work_location: job?.location || '',
    joining_date: '',
    ctc_annual: 0,
    ctc_breakup: {
      basic_salary: 0,
      hra: 0,
      special_allowance: 0,
      performance_bonus: 0
    },
    reporting_manager: '',
    probation_months: 6,
    notice_period_days: 30,
    send_email: true
  });

  const [previewData, setPreviewData] = useState(null);
  const [isLoadingPreview, setIsLoadingPreview] = useState(false);
  const [isSubmitting, setIsSubmitting] = useState(false);

  // Calculate total CTC from breakup
  useEffect(() => {
    const total = Object.values(formData.ctc_breakup).reduce((sum, val) => sum + (parseFloat(val) || 0), 0);
    setFormData(prev => ({ ...prev, ctc_annual: total }));
  }, [formData.ctc_breakup]);

  // Load preview when form is complete
  useEffect(() => {
    if (isFormValid()) {
      loadPreview();
    }
  }, [
    formData.designation,
    formData.joining_date,
    formData.ctc_annual,
    formData.reporting_manager,
    formData.department,
    formData.work_location
  ]);

  const isFormValid = () => {\n    return (\n      formData.designation &&\n      formData.joining_date &&\n      formData.ctc_annual > 0 &&\n      formData.reporting_manager &&\n      formData.department &&\n      formData.work_location\n    );\n  };\n\n  const loadPreview = async () => {\n    if (!isFormValid()) return;\n    \n    setIsLoadingPreview(true);\n    try {\n      const token = localStorage.getItem('token');\n      const response = await axios.post(\n        `${BACKEND_URL}/api/lifecycle/onboarding-preview`,\n        {\n          candidate_id: candidate.id,\n          ...formData\n        },\n        { headers: { Authorization: `Bearer ${token}` } }\n      );\n      setPreviewData(response.data);\n    } catch (error) {\n      console.error('Failed to load preview:', error);\n    } finally {\n      setIsLoadingPreview(false);\n    }\n  };\n\n  const handleSubmit = async () => {\n    setIsSubmitting(true);\n    try {\n      const token = localStorage.getItem('token');\n      const response = await axios.post(\n        `${BACKEND_URL}/api/lifecycle/send-onboarding`,\n        {\n          candidate_id: candidate.id,\n          ...formData\n        },\n        { headers: { Authorization: `Bearer ${token}` } }\n      );\n\n      if (response.data.success) {\n        alert(`Appointment letter ${response.data.email_sent ? 'sent' : 'generated'} successfully!`);\n        onSuccess?.();\n        onClose();\n      }\n    } catch (error) {\n      console.error('Failed to send onboarding letter:', error);\n      alert('Failed to process onboarding: ' + (error.response?.data?.detail || error.message));\n    } finally {\n      setIsSubmitting(false);\n    }\n  };\n\n  const updateCTCComponent = (key, value) => {\n    setFormData({\n      ...formData,\n      ctc_breakup: {\n        ...formData.ctc_breakup,\n        [key]: parseFloat(value) || 0\n      }\n    });\n  };\n\n  const addCTCComponent = () => {\n    const newKey = prompt('Enter component name (e.g., transport_allowance):');\n    if (newKey) {\n      setFormData({\n        ...formData,\n        ctc_breakup: {\n          ...formData.ctc_breakup,\n          [newKey]: 0\n        }\n      });\n    }\n  };\n\n  const removeCTCComponent = (key) => {\n    const { [key]: removed, ...rest } = formData.ctc_breakup;\n    setFormData({\n      ...formData,\n      ctc_breakup: rest\n    });\n  };\n\n  const LeftPane = () => (\n    <div className=\"space-y-6\">\n      {/* Candidate Info */}\n      <div className=\"bg-gray-50 rounded-lg p-4 border border-gray-200\">\n        <h4 className=\"text-sm font-semibold text-gray-700 mb-2\">Candidate</h4>\n        <p className=\"text-base font-medium text-gray-900\">{candidate?.name}</p>\n        <p className=\"text-sm text-gray-600\">{candidate?.email}</p>\n      </div>\n\n      {/* Position Details */}\n      <div className=\"grid grid-cols-2 gap-4\">\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Designation *</label>\n          <input\n            type=\"text\"\n            value={formData.designation}\n            onChange={(e) => setFormData({ ...formData, designation: e.target.value })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            placeholder=\"Senior Software Engineer\"\n          />\n        </div>\n\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Department *</label>\n          <input\n            type=\"text\"\n            value={formData.department}\n            onChange={(e) => setFormData({ ...formData, department: e.target.value })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            placeholder=\"Engineering\"\n          />\n        </div>\n      </div>\n\n      {/* Location and Manager */}\n      <div className=\"grid grid-cols-2 gap-4\">\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Work Location *</label>\n          <input\n            type=\"text\"\n            value={formData.work_location}\n            onChange={(e) => setFormData({ ...formData, work_location: e.target.value })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            placeholder=\"Bangalore, India\"\n          />\n        </div>\n\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Reporting Manager *</label>\n          <input\n            type=\"text\"\n            value={formData.reporting_manager}\n            onChange={(e) => setFormData({ ...formData, reporting_manager: e.target.value })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            placeholder=\"John Doe\"\n          />\n        </div>\n      </div>\n\n      {/* Joining Date */}\n      <div>\n        <label className=\"block text-sm font-medium text-gray-700 mb-2\">Joining Date *</label>\n        <input\n          type=\"date\"\n          value={formData.joining_date}\n          onChange={(e) => setFormData({ ...formData, joining_date: e.target.value })}\n          className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n          min={new Date().toISOString().split('T')[0]}\n        />\n      </div>\n\n      {/* CTC Breakup (Annexure A) */}\n      <div className=\"border-t border-gray-200 pt-6\">\n        <div className=\"flex items-center justify-between mb-4\">\n          <h4 className=\"text-sm font-semibold text-gray-900\">CTC Breakup (Annexure A)</h4>\n          <button\n            type=\"button\"\n            onClick={addCTCComponent}\n            className=\"flex items-center space-x-1 text-sm text-indigo-600 hover:text-indigo-700\"\n          >\n            <Plus size={16} />\n            <span>Add Component</span>\n          </button>\n        </div>\n\n        <div className=\"space-y-3\">\n          {Object.entries(formData.ctc_breakup).map(([key, value]) => (\n            <div key={key} className=\"flex items-center space-x-2\">\n              <div className=\"flex-1\">\n                <label className=\"block text-xs font-medium text-gray-600 mb-1\">\n                  {key.replace(/_/g, ' ').toUpperCase()}\n                </label>\n                <input\n                  type=\"number\"\n                  value={value}\n                  onChange={(e) => updateCTCComponent(key, e.target.value)}\n                  className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n                  placeholder=\"0\"\n                  min=\"0\"\n                  step=\"1000\"\n                />\n              </div>\n              <button\n                type=\"button\"\n                onClick={() => removeCTCComponent(key)}\n                className=\"mt-6 p-2 text-red-600 hover:bg-red-50 rounded-lg transition-colors\"\n              >\n                <Trash2 size={16} />\n              </button>\n            </div>\n          ))}\n        </div>\n\n        <div className=\"mt-4 pt-4 border-t border-gray-200\">\n          <div className=\"flex justify-between items-center\">\n            <span className=\"text-sm font-semibold text-gray-700\">Total Annual CTC</span>\n            <span className=\"text-lg font-bold text-indigo-600\">\n              ₹ {formData.ctc_annual.toLocaleString('en-IN')}\n            </span>\n          </div>\n        </div>\n      </div>\n\n      {/* Terms */}\n      <div className=\"grid grid-cols-2 gap-4\">\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Probation Period (months)</label>\n          <input\n            type=\"number\"\n            value={formData.probation_months}\n            onChange={(e) => setFormData({ ...formData, probation_months: parseInt(e.target.value) || 6 })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            min=\"0\"\n            max=\"12\"\n          />\n        </div>\n\n        <div>\n          <label className=\"block text-sm font-medium text-gray-700 mb-2\">Notice Period (days)</label>\n          <input\n            type=\"number\"\n            value={formData.notice_period_days}\n            onChange={(e) => setFormData({ ...formData, notice_period_days: parseInt(e.target.value) || 30 })}\n            className=\"w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent\"\n            min=\"0\"\n            max=\"90\"\n          />\n        </div>\n      </div>\n\n      {/* Send Email Option */}\n      <div className=\"flex items-start\">\n        <input\n          type=\"checkbox\"\n          id=\"send_email_onboarding\"\n          checked={formData.send_email}\n          onChange={(e) => setFormData({ ...formData, send_email: e.target.checked })}\n          className=\"mt-1 h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded\"\n        />\n        <label htmlFor=\"send_email_onboarding\" className=\"ml-3 text-sm text-gray-700\">\n          <span className=\"font-medium\">Send email with PDF attachment</span>\n          <p className=\"text-gray-500\">Candidate will receive the appointment letter via email</p>\n        </label>\n      </div>\n    </div>\n  );\n\n  const RightPane = () => (\n    <div>\n      {isLoadingPreview ? (\n        <div className=\"flex items-center justify-center h-64\">\n          <div className=\"animate-spin rounded-full h-12 w-12 border-b-2 border-indigo-600\"></div>\n        </div>\n      ) : (\n        <PDFPreviewPane previewData={previewData} />\n      )}\n    </div>\n  );\n\n  return (\n    <DualPaneModal\n      isOpen={isOpen}\n      onClose={onClose}\n      title=\"Onboarding Workflow - Appointment Letter\"\n      leftPane={LeftPane}\n      rightPane={RightPane}\n      onSubmit={handleSubmit}\n      submitButtonText=\"Generate & Send Letter\"\n      isSubmitting={isSubmitting}\n      formData={formData}\n      onFormChange={setFormData}\n      previewData={previewData}\n    />\n  );\n};\n\nexport default OnboardingWorkflow;\n